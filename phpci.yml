build_settings:
    clone_depth: 1 # depth of 1 is a shallow clone, remove this line to clone entire repo

setup:
    shell :
      command: "/home/package_check/package_check.sh %BUILD_PATH%"
        
complete:
    email:
        default_mailto_address: abld@tutanota.com
